[build-system]
requires = ["uv_build<1.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-root = ""
module-name = ["mylib", "optype"]

[project]
name = "private-reexport-pyi"
version = "0.1.0"
requires-python = ">=3.14"
